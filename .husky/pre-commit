# crag:auto-start
#!/bin/sh
# Generated from governance.md by crag
# Regenerate: crag compile --target husky
set -e

# typecheck
npx tsc --noEmit || exit 1

# lint & format
npx biome check --write . || exit 1
npx biome check . || exit 1

# test
npm test || exit 1

# crag:auto-end
